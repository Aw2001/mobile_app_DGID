class User {
     String? identifiant;
     String? prenom;
     String? nom;
     String? username;
     String? password;
     String? role;

     User(this.identifiant, this.prenom, this.nom, this.username, this.password, this.role);

}